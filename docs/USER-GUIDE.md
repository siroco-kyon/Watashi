# Watashi 利用者ガイド

エンドユーザー (管理者以外の社員) 向けの使い方ガイド。

- [初回起動](#初回起動)
- [日常利用](#日常利用)
- [個人設定](#個人設定)
- [ファイル操作](#ファイル操作)
- [パスワード変更](#パスワード変更)
- [このPCを記憶する (自動ログイン)](#このpcを記憶する-自動ログイン)
- [ログアウト](#ログアウト)
- [メンテナンス中の利用](#メンテナンス中の利用)
- [困ったとき](#困ったとき)

---

## 初回起動

起動時にメンテナンス画面が表示された場合は、[メンテナンス中の利用](#メンテナンス中の利用)を確認してください。

1. 配布された URL (例: `https://watashi.internal/install/Watashi.Client.application`) をブラウザで開きインストール (初回のみ)
2. スタートメニュー → 「Watashi」を起動
3. 起動すると **ログイン画面** が表示される（接続先サーバは配布時に設定済みのため、利用者が入力する項目はありません）

> 💡 **起動は必ずスタートメニュー / デスクトップの「Watashi」から。**
> 起動のたびに自動でバージョン確認が行われ、新しい版があればその場で適用されてから起動します (操作は不要)。

```
┌──────────────────────────────┐
│ ユーザー名: [____________]   │
│ ☐ このPCを記憶する           │
│       [次へ]                 │
│ [接続テスト]                 │
└──────────────────────────────┘
```

4. **ユーザー名 (GID)** を入力して「次へ」

- 「**このPCを記憶する**」にチェックを入れると次回から自動ログイン (HTTP/HTTPS どちらでも利用可、HTTP では平文通信の警告ツールチップが付く)

「次へ」を押すと、次のどちらかの画面に進みます。

> ユーザー名の英字は大文字小文字を区別しません。たとえば `g012345` と `G012345` は同じユーザーとして扱われます。

### はじめて使うとき (初回パスワードの設定)

まだパスワードを決めていない場合、**「初回パスワードの設定」** 画面が出ます。Windows のログオン情報でご本人であることが自動的に確認されるため、管理者から初期パスワードを受け取る必要はありません。

```
┌──────────────────────────────────────┐
│ 初回パスワードの設定                 │
│ G012345 さん、ようこそ。             │
│                                      │
│ 新しいパスワード:     [__________]   │
│ 新しいパスワード(確認):[__________]  │
│      [設定してログイン]              │
└──────────────────────────────────────┘
```

- 12 文字以上、英大文字 + 英小文字 + 数字 + 記号を必ず含むこと
- 例: `Spring2026!Welcome`
- ここで決めたパスワードがそのまま本パスワードになります (直後にもう一度変更を求められることはありません)
- 設定するとそのままメイン画面に進みます

> 💡 この画面が出ず、いきなりパスワード入力欄が出た場合は、ご利用の PC で Windows の本人確認ができない環境です。管理者に連絡して初期パスワードを発行してもらってください。

### 2 回目以降 (通常のログイン)

パスワード入力欄が出ます。

- パスワード欄の右端の **👁 目玉アイコン** を押すと、入力中のパスワードを平文表示できる (打ち間違い確認用)。もう一度押すと再びマスク
- パスワード残り日数が 14 日以内なら警告メッセージ
- 有効期限切れ / 管理者がパスワードを発行した直後は、**パスワード変更画面** が強制表示されます

5. **メイン画面** に進む

---

## 日常利用

画面上部で案内と転送先を確認し、左右の一覧で対象を選びます。

| 場所 | 使い方 |
|---|---|
| 上部の案内 | メンテナンス状態、予定時刻、最終確認を確認 |
| 転送バー | アップロード／ダウンロード、選択件数・転送先、転送センター、操作・キーの案内 |
| 左ペイン | このPCのフォルダー。ダウンロードの保存先 |
| 右ペイン | 接続場所とリモートフォルダー。アップロードの保存先 |
| 各ペインの操作 | 新規フォルダー、操作メニュー、パス欄、絞り込み欄。見出しに「操作中」と表示 |
| 下部 | 接続・メンテナンス状態、直近の操作結果、転送状況 |

- **テーマ**: メイン画面右上の月／太陽ボタンでライト／ダークを切り替え。設定は次回起動時のログイン画面にも適用
- 管理者向けの別ウィンドウは小さい画面でも本文全体をスクロールできます。権限の追加・編集と「広く開く」の操作は[管理者ガイド](ADMIN-GUIDE.md#管理画面の開き方)を参照してください。
- **ヘッダー**: 鳥居ロゴ + Watashi、右上にユーザー名・プロトコル・月／太陽のテーマ切替・個人設定・管理ボタン(管理者のみ)・更新・ログアウト・情報
- **左ペイン (ローカル)** 💻: あなたの PC のフォルダ
- **右ペイン (リモート)** ⛩: 中央サーバー経由で見える CIFS 共有 (場所ドロップダウンで切替)
- **リモート一覧**: 最初に 2000 件を表示し、残りがある場合だけ **さらに読み込む** で 2000 件ずつ追加
- **場所の全文**: リモートの場所ドロップダウンへマウスを置くと、現在選択中の表示名・ホスト・共有・許可ルートを表示
- **行ホバー** で長いファイル名・パスがツールチップで全文表示
- 最大化は現在のモニターの作業領域全体を使い、復元ボタンで最大化前の大きさに戻る
- **F5** で両ペイン同時更新、**Backspace** で親フォルダ、**Delete** で削除(確認ダイアログ付き)
- ステータスバー: 接続・メンテナンス状態、直近の操作/エラー (時刻 + 発生元)、コンパクトな転送状況。利用者名はヘッダーに表示

---

### 一覧の読み込みと選択

リモート一覧は初回・追加とも2000件です。絞り込みは読み込み済みの項目だけが対象で、未取得分を含む全体検索ではありません。同じ場所の再読込では選択とスクロール位置を保持します。

### キーボードで操作する

| キー | 動作 |
|---|---|
| Ctrl+D | 右の選択項目を左の現在のフォルダーへダウンロード |
| Ctrl+U | 左の選択項目を右の現在のフォルダーへアップロード |
| F6 / Shift+F6 | 左右の一覧を切替 |
| Ctrl+L / Ctrl+F | 操作中のペインのパス欄／絞り込み欄へ |
| Ctrl+Shift+N | 操作中のペインに新規フォルダー |
| Esc（絞り込み欄） | 絞り込みを解除して一覧へ |
| F1 | 操作・ショートカット案内 |

転送方向は操作中のペインで変わりません。入力欄・IME変換中・長押しでは転送キーを実行しません。対象なし、権限なし、一覧読込中、メンテナンス中は転送できません。選択件数・転送先を確認してください。F2は単一選択、Deleteは複数選択に対応し、削除確認はキャンセルが初期選択です。ローカルは設定によりWindowsのごみ箱、リモートは完全削除です。

## 個人設定

メイン画面右上の **⚙ 個人設定** から、このWindowsユーザーで使用する設定を変更できます。

| 分類 | 設定 | 動作 |
|---|---|---|
| 起動 | ローカルペイン | 前回開いていたフォルダ、または指定した固定フォルダから開始 |
| 起動 | リモートペイン | 未選択、前回開いていた場所、指定したお気に入りから開始 |
| 表示 | テーマ | ライト／ダーク。右上の月／太陽ボタンでも即時切替可能 |
| 表示 | 並び順 | オンにするとローカル／リモートで最後に選んだ列と昇順／降順を記憶 |
| 表示 | 種類別カラー | ファイル名の色分けを有効化し、ルール名・対象拡張子・色・有効状態を編集 |
| ファイル操作 | ローカル削除 | Windowsのごみ箱を使うか、完全削除するかを選択 |

- 固定ローカルフォルダは「現在地」または「参照...」から指定できます。保存時点で存在しないパスは登録できません
- 起動後に固定フォルダが利用できなくなった場合は、前回のフォルダまたはWindowsユーザーフォルダを表示して警告します。固定設定自体は残るため、次回利用可能になれば再び開きます
- 指定したリモートのお気に入りや前回場所は、現在の権限内にある場合だけ開きます。権限がなくなった保存場所は自動的に除外されます
- 「最近使ったリモート場所を消去」は履歴と前回場所を削除しますが、お気に入りは残します
- 「個人設定を初期値に戻す」は、開始位置・テーマ・種類別カラー・削除方法・並び順設定を戻します。お気に入りと履歴は削除しません

設定は `%LocalAppData%\Watashi\settings.json` に保存され、WatashiのログインIDではなく、使用中のWindowsユーザーごとに適用されます。

---

## バージョン情報

ヘッダーの「バージョン情報」から、サイズ変更とスクロールに対応した専用画面を開けます。表示中のClientの実際のバージョンを確認し、「バージョン情報をコピー」でアプリ名とバージョンを問い合わせ用にコピーできます。鳥居の「ひとこともらう」を押すと、ごあいさつや操作のヒントが順番に表示されます。音や自動アニメーションはありません。

## ファイル操作

### フォルダ移動

- 一覧の項目を **ダブルクリック** で展開
- `..` で親フォルダに戻る (権限の最上位では `..` がグレー)
- パス欄に直接入力 + Enter でジャンプも可能
- ローカル・リモートとも、Enterで開く、Backspace／Alt+↑で親へ戻る、Alt+←／→で履歴移動、F5で更新した後も上下キーで操作を続けられます。親へ戻ると元のフォルダを選択し、履歴移動では以前の選択とスクロール位置を表示中の項目から復元します。更新では選択とスクロール位置を維持します。読み込み中に別のペイン・入力欄や別ウィンドウへ移った場合は、フォーカスを奪い返しません。

- ローカルペインのパス欄の隣の **📂 ボタン** で Windows のフォルダ選択ダイアログ (エクスプローラ風) が開き、ツリーから目的のフォルダを選んで一発でジャンプできる
- 別フォルダへの移動が成功すると、そのペインの**絞り込み欄だけ**空になる。列見出しで選んだ並び順は維持
- 列見出し (**名前 / 種類 / サイズ / 更新**) をクリックで並べ替え、同じ見出しをもう一度クリックで昇順 ⇄ 降順。**種類**は拡張子順で、フォルダーと拡張子のないファイルは昇順で先頭にまとまる

パス欄のEnterや移動ボタン、お気に入り・最近使った場所からの移動も、成功後は一覧で操作を続けられます。F6／Shift+F6と絞り込み欄のEscでは一覧の選択行へ戻ります。名前変更・新規フォルダ作成後は対象行を選び、削除後は次の残存行、なければ前の行を選びます。空一覧では一覧自体にフォーカスを置きます。読み込み失敗で一覧を操作できない場合は、パス欄で修正やF5による再試行ができます。入力欄でのBackspace・Altキー操作やIME入力は一覧操作として実行しません。入力欄でF5を押した場合は入力先を維持します。

### アップロード (PC → リモート)

1. 右ペインで送信先の場所とフォルダーを開きます。
2. 左ペインで送りたいファイル／フォルダーを選択します。Ctrl／Shift＋クリックで複数選択できます。
3. 上部の選択件数と転送先を確認して、**アップロード** または `Ctrl+U` を押します。
4. フォルダーや同名項目の確認が表示された場合は内容を確認します。キューへの追加後は、転送センターで完了を確認します。

右クリックのアップロードでも同じ操作です。転送中もキューへ追加できます。転送先は受付時の場所に固定されます。

### ダウンロード (リモート → PC)

1. 左ペインで保存先のフォルダーを開きます。
2. 右ペインで取り出したいファイル／フォルダーを選択します。
3. 上部の選択件数と転送先を確認して、**ダウンロード** または `Ctrl+D` を押します。
4. 確認内容に応答し、転送センターで完了を確認します。保存先は左の現在のフォルダーです。

転送センターは一覧を広く表示し、「詳細を開く」で転送元・先とエラーを確認します。高さが足りない画面では「一覧へ戻る」で切り替え、広い画面では境界をドラッグして一覧と詳細の高さを調整できます。画面全体と詳細はスクロールでき、長いパスやエラー本文は選択してコピーできます。状態フィルターには件数を表示し、「要対応」から失敗・一時停止・競合待ちを確認できます。

ダウンロードは一時ファイルへ書き込み、SHA-256検証後に正式名へ原子的に切り替えます。通信断では確定済みの再開位置を保持します。「完了・スキップの履歴を消去」は再開情報を残して整理します。「失敗・中止の履歴を破棄」は確認後に対象の履歴と途中データを消去し、再開できなくなります。「すべて中止」と履歴整理は絞り込みに関係なく対象件数を確認し、確認時に対象だった転送のみを処理します。

### 転送センターと競合の解決

ホーム画面下部の高さ28pxの転送状況欄で、同時実行中の転送（通常は最大2件）の方向・進捗バー・割合を確認できます。狭い画面ではファイル名を省略し、マウスを重ねるとファイル名・転送元／先・転送済み容量を確認できます。待機（再試行待ち・メンテナンス待ちを含む）と要対応（失敗・一時停止・競合待ち）の件数も表示します。クリック、またはTabで選んでEnter／Spaceを押すと転送センターが開きます。センターの絞り込みに関係なく実行中の転送を表示し、中止処理中は「中止中」、実行中・待機・要対応がないときは「転送なし」と表示します。

上部の転送ボタンは小さくし、選択件数と転送先は引き続き表示します。使用できない理由は各転送ボタンのツールチップで確認できます。

「転送センター」はメイン画面と同時に操作できます。すべて／実行中・待機／要対応／完了・中止で絞り込み、選択項目の転送元・先、サイズ、日時、再試行時刻、エラーを確認できます。同名・更新競合は「競合待ち」となり、内容を確認して上書き・スキップ・別名を選びます。

通信断は上限付きで自動再試行します。期限切れのuploadは失敗として表示され、「再試行」で先頭から再送します。メンテナンス待機は復旧と更新確認後に再開します。一時停止・競合待ち・中止を自動で解除することはありません。

### 削除

1. 削除するペインの一覧で対象を選択します。Ctrl／Shift＋クリックで複数選択できます。
2. **操作 → 削除**、右クリックの削除、または `Delete` を選びます。
3. 場所・件数・全対象名と削除方法を確認します。初期選択はキャンセルです。
4. 全件成功時は一覧が更新され、成功通知は表示されません。失敗がある場合は成功／失敗件数と項目ごとの結果が表示されます。

ローカルは個人設定によりWindowsのごみ箱へ移動するか完全削除します。リモートは完全削除で、Watashiから復元できません。

削除が全件成功した場合は成功通知を表示せず、一覧を更新します。削除の確認と、失敗・一部失敗時の結果、および一覧の再読み込みエラーは引き続き表示します。

### リモート操作の範囲

リモート間コピー・移動のUIはありません。リモートごみ箱・復元もありません。

### リネーム / フォルダ作成

- **リネーム**: `F2` キーで1件だけ選択したファイル/フォルダ名を変更 (右クリック → リネーム... でも同じ)
  - 同じフォルダ内でのリネームのみ可。`/` `\` は名前に含められない
  - リモートでは「許可ルートそのもの (例: `/dept-A`)」と「親ディレクトリ変更」はサーバが拒否する
- **新規フォルダ**: 各ペインの **新規フォルダー**、右クリック、または `Ctrl+Shift+N`
  - フォルダー名を入力して作成します

### 右クリックメニュー

ファイル一覧の上で右クリックすると、選択中アイテムに対する操作が一覧で出る:

| メニュー | ローカル | リモート | 内容 |
|---|---|---|---|
| 開く / 展開 | ✓ | ✓ | フォルダなら展開、`..` なら親へ |
| アップロード / ダウンロード | アップロード | ダウンロード | ツールバーのボタンと同じ動作 |
| リネーム... (F2) | ✓ | ✓ | 同じフォルダ内で名前変更 |
| 削除 (Del) | ✓ | ✓ | 確認ダイアログ付き |
| 新規フォルダ... | ✓ | ✓ | フォルダ名のプロンプト |
| エクスプローラで表示 | ✓ | — | Windows のエクスプローラで選択状態で開く |

### 制約事項 (権限による)

- 自分の権限テンプレートで許可されていない操作はエラー
- **許可ルートそのものは削除/リネーム不可** (例: `/dept-A` 自体は消せない、中身は OK)
- **リネームでは親ディレクトリ変更不可** (フォルダを跨いだ移動は禁止)

---

## パスワード変更

通常時は、メイン画面右上の **🔑 ボタン (パスワード変更)** からいつでも変更できます。
現在のパスワード、新しいパスワード、確認を入力して「変更」を押してください。

有効期限切れ / 管理者が初期パスワードを発行した場合は、ログイン後に同じ画面が強制表示され、変更が完了するまでメイン画面へ進めません。アプリを開いたまま有効期限を迎えた場合も、次の認証更新時に強制変更画面へ切り替わります。

パスワードを忘れた場合は管理者に連絡してください。対応は 2 通りあります。

- **初回設定に戻してもらう** — もう一度「初回パスワードの設定」画面が出て、ご自身で決め直せます (パスワードを人づてに受け取る必要がありません)
- **初期パスワードを発行してもらう** — Windows の本人確認ができない環境の場合。受け取ったパスワードで一度ログインし、その場で変更します

各入力欄 (現在のパスワード / 新しいパスワード / 確認) はすべて **👁 目玉アイコン** で表示確認可能。
新しいパスワードは「確認」と同じ値を入力してください。

ポリシー:
- 12 文字以上
- 英大文字 1 文字以上
- 英小文字 1 文字以上
- 数字 1 文字以上
- 記号 1 文字以上

NG 例: `password123`, `Short1!`, `nouppercases123!`
OK 例: `Spring2026!Welcome`, `Watashi#Tool-2026`

---

## このPCを記憶する (自動ログイン)

ログイン画面の **このPCを記憶する** を ON にしてログインすると、
次回起動から **PC 名 + Windows ユーザー名 + デバイストークン** で自動ログイン。

仕様:
- **HTTP/HTTPS どちらでも利用可能** (HTTP では平文通信なので、ツールチップに警告)
- 1 ユーザーあたり既定 3 台（管理者が 1〜20 台で設定）まで登録可能。同じ端末を再登録したときだけ古いトークンを失効
- Windows Credential Manager に暗号化保存 (他の Windows ユーザーからは読めない)
- ヘッダーの **信頼端末** から自分の登録端末を一覧し、不要な端末だけを失効可能
- 管理者が「全デバイス失効」を行うと無効化される (退職者対応など)
- HTTP 環境では本番想定では非推奨。`Auth:AllowHttpForAutoLogin` が `false` のサーバーには HTTP 自動ログインを拒否される

サーバーで Windows SSO が有効な場合は、ログイン画面の **Windowsアカウントでログイン** からパスワードなしで入れます。無効・不成立の場合も従来のID/パスワードログインはそのまま利用できます。

---

## ログアウト

ヘッダー右上の **⏻ ボタン** をクリック (確認ダイアログ付き)

- サーバー側のリフレッシュトークンを失効
- Credential Manager のデバイストークンも削除
- ログイン画面に戻る

**アイドルタイムアウト**: 操作なしで自動ログアウト。デフォルト 30 分（管理者が社内ポリシーで変更可能）。

パスワード変更・初回設定への差し戻し・管理者権限変更・アカウントロックなどがサーバー側で行われた場合も、安全のため操作中にセッションが切れてログイン画面へ戻ることがあります。表示された理由に従い、必要なら管理者へ連絡してから再ログインしてください。

---

## メンテナンス中の利用

メンテナンス予定・作業中・復旧確認中は起動時や画面上部に案内文、予定時刻、最終確認を表示します。30秒ごとに確認し、手動でも再確認できます。作業中はリモート操作を停止し、転送を「メンテナンス待機」に保持します。Serverの再開と必要な更新確認後に、その待機分だけを再開します。状態確認できない場合も既知の待機は解除しません。案内ページが設定されている場合は画面から開けます。ClickOnceの起動前更新で止まった場合は、管理者から事前に案内された状態ページを開いてください。

予定日時は案内用です。終了予定を過ぎても、利用可能の表示になるまで待ってください。管理者向けの構成手順は[図解付き導入ガイド](../deploy/MAINTENANCE-ROLLOUT-PLAN.html)にあります。

## 困ったとき

| 症状 | 対処 |
|---|---|
| ログイン画面で「アカウントがロックされています」 | 連続で失敗するとロック (既定 15 回)。管理者に連絡してロック解除依頼 |
| 「ユーザー名またはパスワードが違います」が連続 | 大文字小文字、数字混じりをよく確認。CapsLock 注意 |
| 「しばらく時間を置いてから...」「429 Too Many Requests」 | 短時間のログイン試行が多すぎ。1 分待ってから再試行 |
| パスワード変更画面が出る | 90 日経過/管理者リセット。新しいパスワードを設定 |
| 操作中に「パスワードが変更されたため、セッションが無効」と出る | パスワード変更・管理者権限変更・初回設定への差し戻しが反映された。新しい状態で再ログイン |
| 操作中に「アカウントがロック」と出る | 発行済みセッションも即時停止される。管理者にロック解除を依頼してから再ログイン |
| リモートペインが空 | 自分に権限が割り当てられていない可能性。管理者に依頼 |
| 「ホスト/共有が見つかりません」 | サーバー設定で削除された可能性。管理者に確認 |
| 「リネームでは親ディレクトリを変更できません」 | 仕様。同じフォルダ内でのリネームのみ可 |
| 大きなファイルが途中で止まる | 転送センターで状態と次回再試行時刻を確認。一時障害は自動再試行され、アプリ再起動後も確定済み位置から再開可能 |
| 自動ログインがすぐ切れる | サーバーが HTTP 自動ログインを許可していない可能性。管理者に `Auth:AllowHttpForAutoLogin` を確認、または HTTPS で接続し直す |
| 自動ログイン中にいきなり「再ログインしてください」 | パスワード/権限/ロック状態の変更、端末失効、または盗難検知。表示理由を管理者に伝え、必要なら信頼デバイスを再登録 |
| 起動時に更新の進捗ダイアログが出る | ClickOnce 自動更新。新しい版を適用してから起動する (必須更新のためスキップ不可) |
| 起動しても新しい版にならない | 配布サーバーに到達できない可能性。しばらくしてからスタートメニュー / デスクトップの「Watashi」ショートカットから起動し直す |
