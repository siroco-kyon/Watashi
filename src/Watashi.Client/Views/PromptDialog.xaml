<Window x:Class="Watashi.Client.Views.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctl="clr-namespace:Watashi.Client.Controls"
        Title="入力"
        Width="400" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Icon="{StaticResource ToriiIcon}"
        Background="{StaticResource BgBrush}">
    <Border Margin="14" Style="{StaticResource Card}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="MessageLabel" Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap"
                       FontWeight="SemiBold"/>
            <TextBox x:Name="InputBox" Grid.Row="1" Margin="0,0,0,8"/>
            <ctl:PasswordRevealBox x:Name="PasswordInput" Grid.Row="1" Margin="0,0,0,8" Visibility="Collapsed"/>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="キャンセル" IsCancel="True" MinWidth="80" Margin="0,0,8,0"/>
                <Button Content="OK" IsDefault="True" Click="OnOk" Style="{StaticResource PrimaryButton}" MinWidth="80"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
