<Window x:Class="Watashi.Client.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Watashi - 起動中"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="True"
        Icon="{StaticResource ToriiIcon}">
    <!-- 外側 Margin は DropShadow の描画余白 -->
    <Border Margin="16"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.25"/>
        </Border.Effect>
        <StackPanel Margin="28,22,28,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                <Image Source="{StaticResource ToriiIcon}" Width="40" Height="40" Margin="0,0,12,0"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Watashi" Style="{StaticResource H1}"/>
                    <TextBlock Text="CIFS ファイル管理" Style="{StaticResource Caption}" Margin="0,-2,0,0"/>
                </StackPanel>
            </StackPanel>

            <ProgressBar x:Name="Progress" Minimum="0" Maximum="100" Height="8" Margin="0,18,0,8"/>

            <DockPanel>
                <TextBlock x:Name="PercentText" DockPanel.Dock="Right" Text="0%" FontWeight="SemiBold"
                           Foreground="{StaticResource AccentBrush}" Margin="10,0,0,0"/>
                <TextBlock x:Name="StatusText" Text="起動準備中..." Style="{StaticResource Muted}"
                           TextTrimming="CharacterEllipsis"/>
            </DockPanel>

            <TextBlock x:Name="VersionText" Style="{StaticResource Caption}"
                       HorizontalAlignment="Right" Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</Window>
