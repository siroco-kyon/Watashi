<Window x:Class="Watashi.Client.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Watashi - ログイン" Width="420" Height="340" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="ユーザー名" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="2" Text="パスワード" Margin="0,0,0,4"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" PasswordChanged="OnPasswordChanged" Margin="0,0,0,10"/>
        <CheckBox Grid.Row="4" Content="このPCを記憶する"
                  IsChecked="{Binding RememberDevice}" IsEnabled="{Binding CanRemember}"
                  ToolTip="{Binding RememberTooltip}" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="5" Text="{Binding StatusMessage}" Foreground="Red" TextWrapping="Wrap"/>
        <Button Grid.Row="6" Content="ログイン" Command="{Binding LoginCommand}" Height="32" Margin="0,8,0,0"
                IsDefault="True" IsEnabled="{Binding IsBusy, Converter={StaticResource NotBool}}"/>
        <Button Grid.Row="7" Content="接続設定" Click="OnOpenSettings" HorizontalAlignment="Left" Margin="0,8,0,0"/>
    </Grid>
</Window>
