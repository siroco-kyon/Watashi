<UserControl x:Class="Watashi.Client.Controls.TransferStatusStrip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Watashi.Client.Controls"
             SizeChanged="OnSizeChanged">
    <Button x:Name="OpenButton" Click="OnOpen" Height="28" MinHeight="0" Padding="6,2"
            HorizontalContentAlignment="Stretch" Style="{StaticResource GhostButton}"
            ToolTip="{Binding Summary}" AutomationProperties.Name="転送状況。転送センターを開く"
            AutomationProperties.HelpText="{Binding CompactDescription}">
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <ItemsControl ItemsSource="{Binding ActiveJobs}" Visibility="{Binding HasActiveJobs, Converter={StaticResource BoolToVis}}">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,10,0" ToolTip="{Binding ProgressDescription}" VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="55"/><ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding DirectionArrow}" Margin="0,0,4,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding FileName}" TextTrimming="CharacterEllipsis" Margin="0,0,6,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCompact, RelativeSource={RelativeSource AncestorType=local:TransferStatusStrip}}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <ProgressBar Grid.Column="2" Value="{Binding Percent, Mode=OneWay}" Minimum="0" Maximum="100" Height="6"
                                         VerticalAlignment="Center" AutomationProperties.Name="{Binding ProgressDescription}"/>
                            <TextBlock Grid.Column="3" Text="{Binding CompactProgress}" Margin="5,0,0,0" MinWidth="32" TextAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Grid.Column="1" Text="{Binding CompactSummary}" VerticalAlignment="Center"
                       MaxWidth="180" TextTrimming="CharacterEllipsis"/>
        </Grid>
    </Button>
</UserControl>
